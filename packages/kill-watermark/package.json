{
  "name": "kill-watermark",
  "version": "0.9.0",
  "private": true,
  "type": "module",
  "scripts": {
    "create-site": "node ./scripts/create-site.js",
    "build": "rollup -c",
    "dev": "rollup --environment BUILD:development -c --watch"
  }
}
