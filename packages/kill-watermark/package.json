{
  "name": "kill-watermark",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rollup -c",
    "dev": "rollup --environment BUILD:development -c --watch"
  },
  "devDependencies": {
    "camelcase": "^8.0.0"
  }
}
